# Copy this file to .env for LOCAL testing only (never commit the real .env)
# In production, set this in the Vercel dashboard instead:
# Project → Settings → Environment Variables

# Required — powers the AI chat only. Weather and market prices work with
# zero configuration (they use keys already bundled in index.html).
ANTHROPIC_API_KEY=your-anthropic-key-here
